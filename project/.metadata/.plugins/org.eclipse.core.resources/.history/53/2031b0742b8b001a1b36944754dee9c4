#include "sha256/axi_sha256.h"
#include "xil_printf.h"

int main(void) {
	char str[10] = "abc";
	s32 hash_status;

	sha256_t test_hash;
	test_hash.msg_size = 3;
	test_hash.msg_ptr = str;

	hash_status = sha256(&test_hash);

	if(hash_status)
		print("Hash FAILURE\n\r")


}
